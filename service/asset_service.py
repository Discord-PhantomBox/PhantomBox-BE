# from model.label_model import TextRequest
# from util import llm_util
#
#
# async def select(label_id : int , asset_request : TextRequest):
#     text = asset_request.text
#     assets = llm_util.select_asset(label_id, text)
#     return None
